# Obsolete Windows controller backends

This folder is intentionally excluded from active compile by `Stride.Input.csproj` (`**/Obsolete/**`).

Inventory here (DirectInput/XInput-era controller paths) is kept for manual review only.
Current Stri-V active scope is SDL/Linux desktop, Windows RawInput desktop keyboard path, and simulated input.

Do not re-enable these files without an explicit controller-backend product decision.
